#pragma once

/**
 * @file
 * @ingroup core_swizio_nodes
 * @brief Provides the `.hpp` forwarding include for the node delegate model registry.
 *
 * The registry implementation remains declared in `NodeDelegateModelRegistry.h`. This
 * compatibility header exposes the same type through the extension convention used by the
 * bundled node-editor code and avoids duplicate declarations.
 */




#include "NodeDelegateModelRegistry.h"
