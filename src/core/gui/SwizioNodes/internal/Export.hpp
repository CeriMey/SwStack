#pragma once

// Symbol visibility helper (kept for parity with the original QtNodes layout).
// This repo mostly builds static targets, so we keep it empty by default.
#ifndef SWIZIO_NODES_PUBLIC
#define SWIZIO_NODES_PUBLIC
#endif

