#pragma once

/**
 * @file
 * @ingroup core_gui
 * @brief Provides the correctly spelled forwarding include for the emoji picker widget.
 *
 * The implementation still lives in `SwEmojiPiker.h` for backward-compatibility reasons.
 * Including this header lets new code depend on the corrected name without duplicating
 * the actual widget declaration or changing the historical implementation file.
 */




// Spelling fix / convenience include.
// The implementation lives in `SwEmojiPiker.h` (historical typo).

#include "SwEmojiPiker.h"
